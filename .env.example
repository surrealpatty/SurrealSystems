# --- App
NODE_ENV=production
PORT=10000

# --- Security
JWT_SECRET=change-me-to-32plus-random-chars
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,https://another-allowed-origin.com
RATE_LIMIT_MAX=100

# --- Database (Postgres)
DATABASE_URL=postgres://user:pass@host:5432/codecrowds

# Optional (DEV ONLY: allow sync alter)
DB_ALTER=false
